image: grafana/k6:latest

setup_script:
  - apk add --no-cache curl jq git npm
  - npm install -g @anthropic-ai/sandbox-runtime@0.0.20
  - k6 version

cache:
  key: kassandra-deps
  paths:
    - node_modules/
